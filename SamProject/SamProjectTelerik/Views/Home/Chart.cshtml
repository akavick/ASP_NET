@using SamProjectTelerik.Controllers



<div class="demo-section k-content wide">

    @(Html.Kendo().Chart()
                        .Name("chart")
                        //.Title("")
                        .Legend(legend => legend.Position(ChartLegendPosition.Bottom))
                        .Series(series =>
                        {
                            foreach (ChartData list in ViewBag.ColumnsDataSource)
                            {
                                series
                                    .Column(list.DataSource)
                                    .Stack(true)
                                    .Color(list.Color)
                                    .Name(list.Name)
                                    ;
                            }

                            foreach (ChartData area in ViewBag.AreaDataSource)
                            {

                            }

                            foreach (ChartData line in ViewBag.LineDataSource)
                            {

                            }

                        })
                        .CategoryAxis(axis => axis.Name("dateAxis").Type(ChartCategoryAxisType.Date)
                            // Align the first two value axes to the left
                            // and the last two to the right.
                            //
                            // Right alignment is done by specifying a
                            // crossing value greater than or equal to
                            // the number of categories.
                            //.AxisCrossingValue(0, 0, 10, 10)
                        )
                        .ValueAxis(axis => axis.Numeric().Name("rateAxis").Title("ставка").Min(0.0).Max(2.0).MajorUnit(0.25))
    //.ValueAxis(axis => axis
    //    .Numeric("km")
    //        .Title("km")
    //        .Min(0).Max(161).MajorUnit(32)
    //)
    //.ValueAxis(axis => axis
    //    .Numeric("mpg")
    //        .Title("miles per gallon")
    //        .Color("#642381")
    //)
    //.ValueAxis(axis => axis
    //    .Numeric("l100km")
    //        .Title("liters per 100km")
    //        .Color("#e5388a")
    //)
    )
</div>

