<div>
    <ejs-accordion id="AmRateApplicationsGridAccordion">
        <e-accordion-accordionitems>
            <e-accordion-accordionitem expanded="true"
                                       header="Заявления на ставку за период"
                                       content="#AmRateApplicationsCrossingGridDiv">
            </e-accordion-accordionitem>
        </e-accordion-accordionitems>
    </ejs-accordion>

    <div id="AmRateApplicationsCrossingGridDiv"
         style="display: none;">

        <ejs-grid id="AmRateApplicationsGrid"
                  dataSource="@ViewBag.AmRateGridDataSource"
                  allowPaging="true"
                  allowReordering="true"
                  allowResizing="true"
                  allowSorting="true"
                  showColumnMenu="true"
                  allowGrouping="true"
                  allowFiltering="true"
                  allowMultiSorting="true"
                  allowExcelExport="true"
                  allowPdfExport="true"
                  allowRowDragAndDrop="true"
                  enableAltRow="true"
                  allowSelection="true"
                  toolbar="@(new List<string> {"Search"})"
                  dataBound="amRateApplicationsGridDataBound">

            <e-grid-pagesettings pageSize="10.0"
                                 pageSizes="@(new[] {"5", "10", "20", "All"})"></e-grid-pagesettings>

            <e-grid-filterSettings mode="Immediate"
                                   immediateModeDelay="200.0"
                                   type="FilterBar"></e-grid-filterSettings>

            <e-grid-columns>
                <e-grid-column field="Number"
                               headerText="Номер"
                               width="30"></e-grid-column>
                <e-grid-column field="Person.FullName"
                               headerText="Сотрудник"
                               width="30"></e-grid-column>
                <e-grid-column field="BeginDate"
                               headerText="Начало"
                               width="40"
                               format="yMMMd"></e-grid-column>
                <e-grid-column field="EndDate"
                               headerText="Завершение"
                               width="40"
                               format="yMMMd"></e-grid-column>
                <e-grid-column field="Rate"
                               headerText="Ставка"
                               width="30"></e-grid-column>
            </e-grid-columns>
        </ejs-grid>

    </div>
</div>





<script>

    function amRateApplicationsGridDataBound(args) 
    {
        this.autoFitColumns();
    }

</script>